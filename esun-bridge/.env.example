# Keep this file outside the Expo app. Do not use an EXPO_PUBLIC_ prefix.
ESUN_CONFIG_PATH=/absolute/path/to/config.ini
PORT=8787
HOST=127.0.0.1

# Required only when HOST is changed to 0.0.0.0 for a physical phone on your LAN.
# ESUN_BRIDGE_TOKEN=replace-with-a-long-random-token
